<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Скачанное обновление лежит в собственном каталоге приложения: в общих
         «Загрузках» при scoped storage приложение не видит даже свой файл. -->
    <external-files-path name="updates" path="Download/" />
</paths>
